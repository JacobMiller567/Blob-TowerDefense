//enemySpeed -= (spd / 4);
//alarm[0] = room_speed;

//hp -= oTowerParent.gun_damage;

//statusPosion = true;
//with (other) instance_destroy(); // Destroy bullet on colliso
//while (statusPoison) {
//alarm[0] = room_speed * 8; // Poison damage after 8 seconds
//}

