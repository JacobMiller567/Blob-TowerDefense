if (global.level == 21)
{
	room_goto(rMap2); // Go to final map)
}


/*
if keyboard_check_pressed(ord("P"))
    {
    paused = !paused;
	
    if paused == false
        {
        instance_activate_all();
        surface_free(paused_ID);
        paused_ID = -1;
        }
    }
if paused == true
    {
	instance_deactivate_all(true);
    alarm[3]++;
    alarm[4]++;
    }
*/
