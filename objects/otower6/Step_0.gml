event_inherited();
/*

if (! shooting ) { // If shooting is false then return and skip the Step event
	return;
}

var _ds_enemies = ds_list_create();
var num = collision_circle_list(x, y, range, oEnemy, false, true, _ds_enemies, false);

if (num > 0) {
	for (var i = 0; i < clamp(num, 0, 10); i++) { // Can hit up to 10 enemies per shot
		_ds_enemies[] i].bullet_damage;		
	}
	
	alarm_set(2, fire_rate);
	can_shoot = false;
	
	

}

*/

